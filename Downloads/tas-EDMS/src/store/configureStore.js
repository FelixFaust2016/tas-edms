import { createStore, combineReducers } from "redux";



export default () => {

    //store

    const store = createStore(
        combineReducers({
          
        })
    );

    return store;
}
